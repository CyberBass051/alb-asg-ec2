# High Avalable, Scalable AWS Architecture

This project showcases a highly available, scalable AWS architecture deployed using Infrastructure as Code (Terraform). It features an Application Load Balancer (ALB) that distributes incoming traffic across a fleet of EC2 instances managed by an Auto Scaling Group (ASG) with target-tracking CPU policies.
